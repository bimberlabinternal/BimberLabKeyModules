<customView xmlns="http://labkey.org/data/xml/queryCustomView">
    <filters>
        <filter column="availability" operator="eq" value="available for transfer"/>
    </filters>
    <sorts>
        <sort column="Id" descending="false"/>
    </sorts>
</customView>